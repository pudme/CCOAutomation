API integrations are not implemented by design. Data is fed to the platform
via the universal import endpoint. If live API connections are added in a
future version, create integration modules here following the pattern in
CLAUDE.md.
